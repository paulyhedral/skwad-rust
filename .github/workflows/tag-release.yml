name: Tag Release

on:
  pull_request:
    types: [closed]
    branches:
      - main

permissions:
  contents: write

jobs:
  tag-release:
    uses: sweetrpg/github-actions/.github/workflows/rust-tag-release.yaml@master
    with:
      release-branch: main
    secrets: inherit
