[package]
name = "skwad"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
default-run = "skwad"

[dependencies]
skwad-core = { path = "../skwad-core" }

[features]
# Enables the gpui window shell. The gpui dependency (git, pinned rev) is
# added when the window is implemented; see change rust-port-foundation
# task 1.5 follow-up.
gui = []
